server:
  port: 8004

#服务别名--注册=zookeeper到注册中心名称
spring:
  application:
    name: cloud-provider-payment
  cloud:
    zookeeper:
      #一个zookeeper的ip + port(zookeeper开放的端口号)
      connect-string: 192.168.111.144:2881