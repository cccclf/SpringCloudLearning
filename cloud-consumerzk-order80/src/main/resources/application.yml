server:
  port: 80

spring:
  application:
    name: cloud-consumer-order
  cloud:
    #注册到zookeeper
    zookeeper:
      #安装zookeeper的虚拟机的ip地址和zookeeper开放的端口号
      connect-string: 192.168.111.144:2181